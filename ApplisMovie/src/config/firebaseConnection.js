import firebase from 'firebase'

var firebaseConfig = {

};
// Template Firebase à remettre avant la mise en route de l applis
// Initialize Firebase
firebase.initializeApp(firebaseConfig);

export default firebase

